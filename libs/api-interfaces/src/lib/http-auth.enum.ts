export enum HttpHeaderName {
  Authorization = 'Authorization',
  XRequestId = 'X-Request-Id',
}

export enum HttpHeaderNameLowercase {
  Authorization = 'authorization',
}

export enum HttpAuthScheme {
  Bearer = 'Bearer',
}
