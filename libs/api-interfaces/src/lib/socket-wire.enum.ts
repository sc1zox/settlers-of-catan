export enum SocketAuthPayloadKey {
  SessionToken = 'sessionToken',
  AccessToken = 'accessToken',
}

export enum SocketIoTransportName {
  WebSocket = 'websocket',
}

export enum SocketIoUrlPathSegment {
  GameNamespace = 'game',
}

export enum SocketGatewayNamespace {
  Game = '/game',
}

export enum SocketIoLobbyRoomPrefix {
  Lobby = 'lobby:',
}
